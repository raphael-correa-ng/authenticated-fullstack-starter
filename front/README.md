## Fullstack-Starter: Frontend

#### Build & run on Docker

<pre>
docker-compose up --build
</pre>

#### Build & run for development

<pre>
export NODE_OPTIONS=--openssl-legacy-provider && npm start
</pre>